name: build python-gphoto2 wheels
on: workflow_dispatch

jobs:
  build:
    name: build ${{ matrix.build }}
    runs-on: ${{ matrix.runner }}
    strategy:
      matrix:
        include:
          - build: Linux-x64
            runner: ubuntu-22.04
            arch: x86_64
            wget: wget
          - build: Linux-arm64
            runner: ubuntu-22.04-arm
            arch: aarch64
            wget: wget
          - build: MacOS-x64
            runner: macos-15-intel
            target: 13.0
            arch: x86_64
            wget: wget
          - build: MacOS-arm64
            runner: macos-14
            dependencies: libtool gd
            target: 14.0
            arch: arm64
            wget: wget
          - build: Windows-x64
            runner: windows-2022
            arch: AMD64
            wget: c:/msys64/usr/bin/wget.exe
    env:
      GPHOTO_DIR: ${{ github.workspace }}/libgphoto2
      GPHOTO_SRC: >
        https://github.com/gphoto/libgphoto2/releases/download/v2.5.33/libgphoto2-2.5.33.tar.xz
      MACOSX_DEPLOYMENT_TARGET: ${{ matrix.target }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
        with:
          repository: jim-easterbrook/python-gphoto2
          ref: devel

      - name: Download libgphoto2
        run: |
          mkdir ${{ env.GPHOTO_DIR }}
          ${{ matrix.wget }} -nv ${{ env.GPHOTO_SRC }} -O - |
          tar --xz -x -f - --directory ${{ env.GPHOTO_DIR }} --strip-components=1

      - name: Build libgphoto2
        if: ${{ runner.os == 'macOS' }}
        run: |
          brew install libexif ${{ matrix.dependencies }}
          cd ${{ env.GPHOTO_DIR }}
          ./configure --prefix=$PWD/local_install --disable-internal-docs \
            --enable-vusb \
            CFLAGS="-D_DARWIN_C_SOURCE -I$(brew --prefix)/include"
          make
          make install

      - name: Build wheels (Python 3.6 & 3.7)
        if: ${{ matrix.runner != 'macos-14' }}
        # cibuildwheel@v3.x omits Python 3.6 & 3.7
        uses: pypa/cibuildwheel@v2.23.3
        env:
          CIBW_ARCHS: ${{ matrix.arch }}
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_MANYLINUX_AARCH64_IMAGE: manylinux_2_28
          CIBW_BUILD: "cp36* cp37*"
          CIBW_SKIP: "*musllinux*"
          CIBW_ENVIRONMENT_LINUX: >
            GPHOTO2_ROOT=$PWD/libgphoto2/local_install
          CIBW_ENVIRONMENT_MACOS: >
            GPHOTO2_ROOT=${{ env.GPHOTO_DIR }}/local_install
            SYSTEM_VERSION_COMPAT=0
          CIBW_TEST_COMMAND: >
            python3 -m gphoto2 -v &&
            python3 -m unittest discover {project}/tests -v
          CIBW_BEFORE_ALL_LINUX: >
            yum install -y libusbx-devel libexif-devel libjpeg-turbo-devel
            gd-devel libcurl-devel libxml2-devel libtool-ltdl-devel &&
            pushd $PWD/libgphoto2 &&
            ./configure --prefix=$PWD/local_install --disable-internal-docs
            --enable-vusb &&
            make &&
            make install &&
            popd

      - name: Build wheels (Python 3.8+)
        uses: pypa/cibuildwheel@v3.4.0
        env:
          CIBW_ARCHS: ${{ matrix.arch }}
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux2014
          CIBW_MANYLINUX_AARCH64_IMAGE: manylinux_2_28
          CIBW_BUILD: "cp*"
          CIBW_SKIP: "*musllinux*"
          CIBW_ENVIRONMENT_LINUX: >
            GPHOTO2_ROOT=$PWD/libgphoto2/local_install
          CIBW_ENVIRONMENT_MACOS: >
            GPHOTO2_ROOT=${{ env.GPHOTO_DIR }}/local_install
            SYSTEM_VERSION_COMPAT=0
          CIBW_TEST_COMMAND: >
            python3 -m gphoto2 -v &&
            python3 -m unittest discover {project}/tests -v
          CIBW_TEST_SKIP: "cp38-macosx_*:arm64"
          CIBW_BEFORE_ALL_LINUX: >
            yum install -y libusbx-devel libexif-devel libjpeg-turbo-devel
            gd-devel libcurl-devel libxml2-devel libtool-ltdl-devel &&
            pushd $PWD/libgphoto2 &&
            ./configure --prefix=$PWD/local_install --disable-internal-docs
            --enable-vusb &&
            make &&
            make install &&
            popd

      - name: Store results
        uses: actions/upload-artifact@v6
        with:
          name: gphoto2-wheels-${{ matrix.build }}
          path: wheelhouse/*.whl
